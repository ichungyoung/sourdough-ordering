# MongoDB Atlas Connection URI
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/karens_kitchen?retryWrites=true&w=majority

# Admin panel authorization token/password (defaults to 'admin' if not set)
ADMIN_PASSWORD=admin

# Server port (defaults to 3000 if not set)
PORT=3000
